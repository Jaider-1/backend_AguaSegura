// test/dummy.spec.ts
describe('Dummy test suite', () => {
  it('should always pass', () => {
    expect(true).toBe(true);
  });

  it('should verify basic math', () => {
    expect(1 + 1).toBe(2);
  });
});